apiVersion: v1
kind: Service
metadata:
  name: portfolio-lb
  namespace: myportfolio
spec:
  type: LoadBalancer
  selector:
    app: portfolio-app
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
